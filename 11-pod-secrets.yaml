apiVersion: v1
kind: Pod
metadata:
 name: pod-using-secrets
spec:
  containers:
   - name: nginx
     image: nginx
     envFrom:
     - secretKeyRef:
        name: secrets  #it refers the complete configmap file expense